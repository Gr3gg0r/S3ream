# Third-Party Notices

hulesa's own source code is licensed under the [MIT License](LICENSE).
It builds on, and its packaged releases bundle, the following third-party
components, which remain the property of their respective owners and are
governed by their own licenses.

## FFmpeg / FFprobe (GPL-3.0)

Packaged builds bundle FFmpeg and FFprobe binaries fetched at install time by
`@ffmpeg-installer/ffmpeg` and `@ffprobe-installer/ffprobe`. Those binaries are
licensed under the **GNU General Public License v3.0** (they are built with GPL
components such as libx264). When you redistribute a packaged hulesa build, the
FFmpeg/FFprobe binaries inside it remain governed by GPL-3.0; hulesa's own code
stays MIT. FFmpeg source code and license texts are available at
<https://ffmpeg.org> and <https://ffmpeg.org/legal.html>.

The `@ffmpeg-installer/ffmpeg` and `@ffprobe-installer/ffprobe` wrapper packages
themselves are licensed under LGPL-3.0.

## Other runtime dependencies

| Component          | License    | Used for                                    |
| ------------------ | ---------- | ------------------------------------------- |
| Electron           | MIT        | Desktop runtime                             |
| React / React DOM  | MIT        | Renderer UI                                 |
| minio (npm client) | Apache-2.0 | S3-compatible uploads                       |
| execa              | MIT        | Process spawning                            |
| dotenv             | BSD-2      | Environment configuration                   |
| Inter (fontsource) | OFL-1.1    | UI typeface                                 |
| Tailwind CSS       | MIT        | Styling                                     |
| DaisyUI            | MIT        | Theme tokens                                |

## Development-only components

- **RustFS** (Apache-2.0) — local S3-compatible server used by the Docker dev
  stack and integration tests. Not bundled into or required by the app.
- **Toxiproxy** (MIT) — slow-network simulation in the dev stack.
